#include <bits/stdc++.h>

using namespace std;

int main() {
    freopen("output.txt", "r", stdin);
    freopen("input.txt", "a", stdout);
    string s;
    while (getline(cin, s)) cout << s << '\n';
    return 0;
}
